1. The idea is to use one hash set to record sum of every digit square of every number occurred. 

2. Once the current sum cannot be added to set, return false; once the current sum equals 1, return true